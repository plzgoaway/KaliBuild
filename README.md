Update and upgrade packages, otherwise sudo token will expire during run\
Clone repo - git clone https://github.com/plzgoaway/KaliBuild\
cd into repo\
sudo whoami\
ansible-galaxy install -r requirements.yml\
ansible-playbook main.yml
